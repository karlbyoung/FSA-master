CREATE OR REPLACE TABLE DEV."BUSINESS_OPERATIONS"."OPEN_PO" (
	ID INT AUTOINCREMENT START 1 INCREMENT 1,
	PO_ROW_NO INT,
	PO_ITEM_ID INT,
	PO_ITEM_TYPE VARCHAR,
	PO_TYPE VARCHAR,
	ITEM_ID INT,
	ITEM VARCHAR,
	ITEM_DISPLAY_NAME VARCHAR,
	ASSEMBLY_ITEM_ID INT,
	ASSEMBLY_ELSE_ITEM_ID VARCHAR,
	ASSEMBLY_ITEM VARCHAR,
	ASSEMBLY_ITEM_DISPLAY_NAME VARCHAR,
	ORDER_NUMBER VARCHAR,
	PURCHASE_ORDER_TRANSACTION_ID INT,
	STATUS VARCHAR,
	LOCATION VARCHAR,
	RECEIVE_BY_DATE DATE,
	NS_RECEIVE_BY_DATE DATE,
	UNIQUE_KEY INT,
	QUANTITY_TO_BE_RECEIVED INT,
	SOURCE_LOAD_DATE DATETIME DEFAULT CURRENT_TIMESTAMP(),
	INSERT_DATE DATETIME DEFAULT CURRENT_TIMESTAMP(),
	INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP()
);