CREATE OR REPLACE TABLE DEV."BUSINESS_OPERATIONS"."DEMAND_PO" (
	ID INT,
	FK_ID INT,
	ORDER_NUMBER VARCHAR, -- CHANGED FROM ORDERNUMBER
	UNIQUE_KEY INT,
	DDA DATE,
	DDA_MODIFIED DATE,
	ORIGINAL_DDA DATE, -- CHANGED FROM ORG_DDA
	SEQUENCING_DDA DATE,
	TRANS_TYPE VARCHAR,
	TOTAL_AMT FLOAT,
	PRIORITY_LEVEL INT,
	NS_LINE_NUMBER INT,
	ITEM VARCHAR,
	ITEM_ID INT,
	COMPONENT_ITEM_ID INT DEFAULT NULL,
	COMPONENT_ITEM VARCHAR DEFAULT NULL,
	QUANTITY INT DEFAULT 0,
	TOTAL_AVAIL_QTY INT DEFAULT 0,
	COMPONENT_QTY_ORDERED INT DEFAULT 0,
	LOCATION VARCHAR,
	BO_STATUS VARCHAR,
	SOURCE_TYPE VARCHAR, -- CHANGED FROM SOURCETYPE
	SOURCE_LOAD_DATE_PREV DATETIME, -- CHANGED FROM SOURCELOADDATE_PREV
	SOURCE_LOAD_DATE_CURRENT DATETIME , -- CHANGED FROM SOURCELOADDATE_CURRENT
	SOURCE_TYPE_DAILY VARCHAR, -- CHANGED FROM SOURCETYPE_DAILY
	PO_SLIPPAGE VARCHAR DEFAULT 'N', -- ADDED BY JACKSON
	INSERT_DATE DATETIME DEFAULT CURRENT_TIMESTAMP(), -- CHANGED FROM INSERTDATE
	INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP() -- CHANGED FROM INSERTDATETIME
)