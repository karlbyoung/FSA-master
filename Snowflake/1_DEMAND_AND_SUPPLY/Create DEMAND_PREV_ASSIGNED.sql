CREATE TABLE IF NOT EXISTS DEV.${FSA_CURRENT_SCHEMA}.DEMAND_PREV_ASSIGNED
	(PK_ID TEXT,
     UNIQUE_KEY FLOAT,
     COMPONENT_ITEM_ID FLOAT,
     ORDER_NUMBER TEXT,
     NS_LINE_NUMBER TEXT,
     HASH_VALUE NUMBER,
     HASH_FSA_OUTPUT NUMBER,
     INSERT_DATE TIMESTAMP_LTZ,
     LAST_MODIFIED TIMESTAMP_LTZ,
     ROW_NO NUMBER,
     PREV_AVAIL_DATE DATE,
     PREV_CAPPING_DDA DATE,
     ORIG_CAP_DDA DATE,
     PO_INDICATOR NUMBER,
     PO_INDICATOR_ASSIGN NUMBER,
     PO_ORDER_NUMBER TEXT,
     PO_RECEIVE_BY_DATE DATE);
