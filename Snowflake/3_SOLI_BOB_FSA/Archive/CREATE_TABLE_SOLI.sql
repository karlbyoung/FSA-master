CREATE OR REPLACE TABLE DEV."BUSINESS_OPERATIONS"."SOLI" (
	ID INT AUTOINCREMENT START 1 INCREMENT 1,
	FK_SPA_ID INT,
	ORDER_NUMBER VARCHAR, -- CHANGED FROM ORDERNO
	UNIQUE_KEY INT,
	NS_LINE_NUMBER INT,
	ITEM_NO VARCHAR,
	ITEM_ID INT,
	RECEIPT_DATE DATETIME,
	FREDD DATETIME,
	SOURCE_LOAD_DATE DATETIME DEFAULT CURRENT_TIMESTAMP(), -- CHANGED FROM SOURCELOADDATE
	INSERT_DATE DATETIME DEFAULT CURRENT_TIMESTAMP(), -- CHANGED FROM INSERTDATE
	INSERT_DATETIME DATETIME DEFAULT CURRENT_TIMESTAMP() -- CHANGED FROM INSERTDATETIME
);